# FastAPI/Uvicorn
# PORT=8000 # Already in run.py as default

# PostgreSQL
POSTGRES_SERVER=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# JWT
SECRET_KEY="your_very_secret_and_unique_key_here_please_change_me"
# ACCESS_TOKEN_EXPIRE_MINUTES=30 (Defaults in core/config.py)
# REFRESH_TOKEN_EXPIRE_DAYS=7 (Defaults in core/config.py)

# First Superuser (Optional, can be created via script or manually)
# Ensure these are strong and unique if you use them.
FIRST_SUPERUSER_EMAIL=admin@example.com
FIRST_SUPERUSER_PASSWORD=changeme

# CORS Origins (Comma separated list, or * for all)
# BACKEND_CORS_ORIGINS="http://localhost,http://localhost:3000,http://localhost:8081" (Defaults to * in core/config.py)

# Add other environment variables for external services like Google API keys later
# GOOGLE_API_KEY=
# GEMINI_API_KEY=
